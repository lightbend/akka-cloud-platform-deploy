extensions:
  health_check:
    endpoint: 0.0.0.0:13133

receivers:
  zipkin:

processors:
  batch:

exporters:
  logging:
    loglevel: debug
  awsxray:

service:
  pipelines:
    traces:
      receivers: [zipkin]
      processors: [batch]
      # exporters: [logging, awsxray]
      exporters: [awsxray]
  extensions: [health_check]
